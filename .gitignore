# Build outputs
/target
/build/
/dist/
/bin/
/out/

# Binaries
*.exe
*.dll
*.so
*.dylib
*.app

# dotvault: .vault and .vault.keys ARE committed (encrypted + public keys).
# Only the lock file is local.
/.vault.lock
*.env

# Local dotvault artifacts (smoke tests run against this repo itself).
/.vault
/.vault.keys
/.dotvault/

# Editor / tool caches
/.zcode/

# Test coverage
/coverage/
*.profraw
*.profdata
